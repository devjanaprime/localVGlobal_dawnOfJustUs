Local v Global: Dawn of Just Us
===============================

Checking if a global variable retains it's value within functions which have local variables of the same name.

This is a bad idea and don't do it in the "real world".
